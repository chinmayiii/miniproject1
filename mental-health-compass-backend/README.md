# Mental Health Compass Backend

## Setup
1. Clone the repo.
2. Install dependencies: `pip install -r requirements.txt`
3. Add your `.env` file with API keys.
4. Run: `python app.py`

## API Endpoints
- `/chat_real` — Chatbot endpoint
- `/predict` — Prediction endpoint